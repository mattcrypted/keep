# Keep — 0G Cup. Copy to .env and fill in. NEVER commit .env.

# Throwaway 0G Galileo testnet wallet private key (0x...). Generate with: npm run wallet
OG_KEY=

# 0G Galileo testnet endpoints (defaults shown — usually no need to change)
OG_RPC=https://evmrpc-testnet.0g.ai
OG_INDEXER=https://indexer-storage-testnet-turbo.0g.ai

# Anthropic API key for the chat "brain" (added in the chat-loop step, not the spike)
ANTHROPIC_API_KEY=
